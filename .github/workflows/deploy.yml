name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: hlaurent
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            set -e
            cd /opt/payfip/backend-src
            git pull origin main
            cd /opt/payfip/deploy
            docker compose -f docker-compose.yml -f docker-compose.prod.yml build gateway svc-auth svc-billetterie svc-factures svc-gestion svc-mail
            docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d gateway svc-auth svc-billetterie svc-factures svc-gestion svc-mail
